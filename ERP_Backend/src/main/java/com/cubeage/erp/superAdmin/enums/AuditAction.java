package com.cubeage.erp.superAdmin.enums;

public enum AuditAction {
    CREATE, UPDATE, DELETE, SUSPEND, ACTIVATE, LOGIN, LOGOUT,
    PLAN_CHANGE, SUBSCRIPTION_RENEW, FEATURE_TOGGLE, SETTING_CHANGE
}
