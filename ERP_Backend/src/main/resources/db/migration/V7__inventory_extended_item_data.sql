-- V7: Extended Inventory Item Data Persistence

ALTER TABLE inventory_items
    ADD COLUMN IF NOT EXISTS item_type VARCHAR(80),
    ADD COLUMN IF NOT EXISTS sub_category VARCHAR(80),
    ADD COLUMN IF NOT EXISTS barcode VARCHAR(100),
    ADD COLUMN IF NOT EXISTS hsn_sac_code VARCHAR(50),
    ADD COLUMN IF NOT EXISTS description TEXT,
    ADD COLUMN IF NOT EXISTS purchase_unit VARCHAR(20),
    ADD COLUMN IF NOT EXISTS sales_unit VARCHAR(20),
    ADD COLUMN IF NOT EXISTS stock_unit VARCHAR(20),
    ADD COLUMN IF NOT EXISTS conversion_factor NUMERIC(19, 4) DEFAULT 1.0,
    ADD COLUMN IF NOT EXISTS storage_location VARCHAR(100),
    ADD COLUMN IF NOT EXISTS maximum_stock_level NUMERIC(19, 3) DEFAULT 0,
    ADD COLUMN IF NOT EXISTS reorder_level NUMERIC(19, 3) DEFAULT 0,
    ADD COLUMN IF NOT EXISTS reorder_quantity NUMERIC(19, 3) DEFAULT 0,
    ADD COLUMN IF NOT EXISTS selling_price NUMERIC(19, 2) DEFAULT 0,
    ADD COLUMN IF NOT EXISTS costing_method VARCHAR(80),
    ADD COLUMN IF NOT EXISTS tax_rate NUMERIC(19, 2) DEFAULT 0,
    ADD COLUMN IF NOT EXISTS primary_supplier VARCHAR(150),
    ADD COLUMN IF NOT EXISTS supplier_item_code VARCHAR(100),
    ADD COLUMN IF NOT EXISTS lead_time INTEGER DEFAULT 0,
    ADD COLUMN IF NOT EXISTS preferred_supplier BOOLEAN DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS taxable_item BOOLEAN DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS gst_rate NUMERIC(19, 2) DEFAULT 18,
    ADD COLUMN IF NOT EXISTS cgst NUMERIC(19, 2) DEFAULT 9,
    ADD COLUMN IF NOT EXISTS sgst NUMERIC(19, 2) DEFAULT 9,
    ADD COLUMN IF NOT EXISTS igst NUMERIC(19, 2) DEFAULT 18,
    ADD COLUMN IF NOT EXISTS tax_hsn_code VARCHAR(50),
    ADD COLUMN IF NOT EXISTS track_inventory BOOLEAN DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS track_batch BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS track_serial_number BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS low_stock_alert BOOLEAN DEFAULT TRUE,
    ADD COLUMN IF NOT EXISTS auto_reorder BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS allow_negative_stock BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS batch_tracking BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS expiry_tracking BOOLEAN DEFAULT FALSE,
    ADD COLUMN IF NOT EXISTS shelf_life INTEGER,
    ADD COLUMN IF NOT EXISTS manufacturing_date DATE,
    ADD COLUMN IF NOT EXISTS expiry_date DATE,
    ADD COLUMN IF NOT EXISTS batch_prefix VARCHAR(50),
    ADD COLUMN IF NOT EXISTS length NUMERIC(19, 3),
    ADD COLUMN IF NOT EXISTS width NUMERIC(19, 3),
    ADD COLUMN IF NOT EXISTS thickness NUMERIC(19, 3),
    ADD COLUMN IF NOT EXISTS weight NUMERIC(19, 3),
    ADD COLUMN IF NOT EXISTS grade_model VARCHAR(100),
    ADD COLUMN IF NOT EXISTS specification TEXT,
    ADD COLUMN IF NOT EXISTS attachments_json TEXT,
    ADD COLUMN IF NOT EXISTS internal_notes TEXT,
    ADD COLUMN IF NOT EXISTS additional_notes TEXT;

CREATE INDEX IF NOT EXISTS idx_inv_items_tenant_cat ON inventory_items(tenant_id, category);
CREATE INDEX IF NOT EXISTS idx_inv_items_tenant_wh ON inventory_items(tenant_id, warehouse_code);
CREATE INDEX IF NOT EXISTS idx_inv_movements_tenant_date ON inventory_stock_movements(tenant_id, movement_date DESC);
